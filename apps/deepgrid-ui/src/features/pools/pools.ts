export type PoolOption = { key: string; label: string };

export const POOLS: PoolOption[] = [
    { key: "SUI_DBUSDC", label: "SUI / DBUSDC" },
    // Add more when ready:
    // { key: "SUI_USDC", label: "SUI / USDC" },
];
